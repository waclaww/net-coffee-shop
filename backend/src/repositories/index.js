const UserRepository = require('./user.repository');
const ProductRepository = require('./product.repository');
const StaticRepository = require('./static.repository');

module.exports = {
    UserRepository,
    ProductRepository,
    StaticRepository }
